#pragma once

// Placeholder - 完整内容由 Task 4 实现
const char INDEX_HTML[] = "<html><body><h1>Loading...</h1></body></html>";
const size_t INDEX_HTML_LEN = sizeof(INDEX_HTML);

// Placeholder processor function - 后续完善
inline String processor(const String& var) {
    return String();
}
